# CI maintenance verification

This branch-only marker exists solely to exercise the WordPress PHP 7.4 compatibility workflow against the merged CI-runtime maintenance changes. It must not be merged.
