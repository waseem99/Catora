# CI maintenance verification

This branch-only marker exists solely to exercise the PostgreSQL migration and backup workflow against the merged CI-runtime maintenance changes. It must not be merged.
